import { Routes } from '@angular/router';
import { InvoiceGeneratorComponent } from './invoice-generator/invoice-generator';

export const routes: Routes = [{ path: '', component: InvoiceGeneratorComponent }];
